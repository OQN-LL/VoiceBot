import discord

class Bot:
    def __init__(self,token,command_prefix="/"):
        self.bot commands.Bot(command_prefix=commands.when_mentioned_or(command_prefix))
        self.token = token

    def run():
        self.bot.run(self.token)

    
